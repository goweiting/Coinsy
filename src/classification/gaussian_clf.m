function pedictions = gaussian_clf(X_test, mean, covariance )
%% GAUSSIAN_CLF(X_TEST, MEAN, COVARIANCE)
%   Given a the features of some images (X_test), we use a gaussian model that %    is fitted to 
