\documentclass[main.tex,tikz]{subfiles}
% CREATE THE FLOWCHART FOR DEMO:

\begin{document}
\begin{tikzpicture} [scale=.2, node distance=.3cm, start chain=going below]
  %% DATA
  \node[punktchain, join] (Data)
    {Given data:
    \begin{itermize}
    \item 9 simpler images
    \item 5 harder images
    \end{itermize}}
  %% PREPROCESSING
  \node[imageprocess, join] (normalisation)
      {Normalisation of image};
  %% SEGMENTATION
  \node[imageprocess, join] (bg_subtract)
      {Background Subtraction};
	\node[imageprocess, join] (thresholding)
      {Thresholding};
	\node[imageprocess, join] (segment)
      {Find edges and extract regions};
	\node[classification, join] (features)
      {Extract properties};
  \node[classification, join] (classifier)
      {Classification of regions};
  \node[classification, join] (counting)
      {Count value of coins};
  \node[punktchain, join] (next_job)
      {Next image};

\end{tikzpicture}

\end{document}
