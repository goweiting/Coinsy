\documentclass[main.tex,tikz]{subfiles}
% CREATE THE FLOWCHART FOR DEMO:

\begin{document}
\begin{tikzpicture} [scale=.2, node distance=.3cm, start chain=going below]
  %% DATA
  \node[punktchain, join] (next_job)
      {Input image};
  %% PREPROCESSING
  \node[imgprocessing, join] (normalisation)
      {Normalisation of image};
  %% SEGMENTATION
  \node[imgprocessing, join] (bg_subtract)
      {Background Subtraction using \verb+bg_model+};
  \node[imgprocessing, join] (thresholding)
      {Thresholding and find edges};
  \node[imgprocessing, join] (region)
      {Extract Regions (subimages)};
  %% Feat extraction
	\node[featextract, join] (features)
      {Extract properties (\verb+X_test+)};
  %% Classsification
  \node[classification, join] (classifier)
      {Classification of \verb+X_test+};
  \node[classification, join] (counting)
      {Output classification results\\Count value of coins};
  \node[punktchain, join] (next_job)
      {Ready for Next image\\(Online update of model - not implemented)};

\end{tikzpicture}

\end{document}
